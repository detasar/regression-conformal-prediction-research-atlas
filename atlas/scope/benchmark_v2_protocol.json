{
  "benchmark_version": "benchmark_v2_prospectively_frozen_protocol",
  "design_requirements": [
    "Use fewer but independently sourced datasets with explicit source/version/license records.",
    "Represent task variants under their source dataset instead of counting them as independent datasets.",
    "Use the same alpha grid, seed count, split policy, learner/config surface, and conformal wrappers within each paired task.",
    "Compare conformal wrappers within exact learner/config/split cells rather than averaging across mismatched model grids.",
    "A fixed CQR backend is counted only within its matched learner/config cells.",
    "Fit imputation, scaling, capping, feature selection, and dimensionality reduction inside each CV+/jackknife fold.",
    "Separate IID, grouped, temporal, spatial, and shift regimes before aggregation.",
    "Freeze the primary utility rule, exclusion rules, and pathology-display policy before compute starts.",
    "Report source-dataset-clustered bootstrap or hierarchical meta-analysis rather than row-level pseudo-inference.",
    "Reserve untouched post-protocol datasets for external validation.",
    "Publish planned, attempted, completed, failed, skipped, and publication-scoped accounting matrices.",
    "Publish enough run-level ledger, split hash, environment lock, and aggregate cube material to audit the synthesis."
  ],
  "generated_at_utc": "2026-07-17T00:49:53.355689+00:00",
  "interpretation_scope": "Benchmark v2 is a forward protocol with a separate resumable execution ledger. It does not revise the Atlas v0 retrospective results until new, separately versioned runs are completed and synthesized.",
  "primary_estimand": {
    "primary_comparison_unit": "source_dataset_task_alpha_learner_config_split_hash",
    "primary_utility_rule": "pre-specified coverage tolerance gate followed by interval score, reported with sensitivity to absolute coverage error and width",
    "unit_hierarchy": [
      "source_dataset",
      "task_variant",
      "split_regime",
      "split_hash",
      "learner_config",
      "conformal_method_config"
    ]
  },
  "purpose": "Define the next balanced, paired, leakage-safe regression conformal prediction benchmark and record the execution controls used after result generation started.",
  "schema": "regression_cp_benchmark_v2_protocol_v1",
  "status": "protocol_defined_execution_started"
}
