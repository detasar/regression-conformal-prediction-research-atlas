# Row Unit Definition

A completed row is one audited result record for a dataset, target, alpha level, conformal method, base-model configuration, split or calibration design, seed, and preprocessing policy after the experiment runner records a completed status.

Rows are not independent scientific replicates. They are accounting units for the empirical result surface. Aggregates in this atlas are descriptive unless a claim gate explicitly says otherwise.
