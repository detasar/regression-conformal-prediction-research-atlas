# Regression CP Research Atlas public requirements
# Convenience constraints for local public smoke checks and dry-run execution.
# Exact public smoke pins are in reproducibility/environment/requirements-public-lock.txt.

# Core public smoke dependencies
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
pyyaml>=6.0
matplotlib>=3.7
loguru>=0.7
pytest>=7.4

# Optional learner backends used by historical sweep configs
xgboost>=2.0
lightgbm>=4.0
catboost>=1.2

# External data execution dependencies
openml>=0.14
fairlearn>=0.10
folktables>=0.0.12
aif360>=0.6
